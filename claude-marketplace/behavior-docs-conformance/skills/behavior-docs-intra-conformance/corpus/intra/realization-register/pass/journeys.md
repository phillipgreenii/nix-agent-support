# Use cases & open questions — sample (realization-register PASS fixture, INV-23)

## Open questions

- **`OQ-1` — Which durability guarantee delivery owes.** The intent itself is undecided, which is
  what makes this an open question rather than a register row. _Owner_: author. _Path_: decide on a
  real deployment. _Blocks_: the durability wording of `INV-1`.

INTRA expectation: CLEAN — this `OQ-` records **unsettled intent**, not a realization gap; the gap
against `INV-1` lives in the register in `README.md` (`INV-23`).
