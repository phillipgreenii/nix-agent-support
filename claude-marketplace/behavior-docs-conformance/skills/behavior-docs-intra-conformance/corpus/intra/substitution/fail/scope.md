# Scope — sample (substitution FAIL fixture, extent well-formedness)

- **Extent** — This set covers `pr-pool-daemon.service` and the `--poll-interval=30s` flag.

INTRA expectation: FLAG on `substitution` — the extent names concrete realization artifacts, so
substituting a generalization does not preserve the intended behavior (the statement IS the
artifact). A well-formed extent survives the substitution test.
