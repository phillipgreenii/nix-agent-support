# Invariants — sample (inline-status PASS fixture, intra #15)

- **`INV-1`** — The core delivers every accepted event at least once (durable across restart).

INTRA expectation: CLEAN — the rule is stated as-if-true (living by default, INV-4).
