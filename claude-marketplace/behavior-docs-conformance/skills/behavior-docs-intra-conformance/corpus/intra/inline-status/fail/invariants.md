# Invariants — sample (inline-status FAIL fixture, intra #15)

- **`INV-1`** — The core delivers every accepted event at least once.
- _Status: unmet by the current implementation._

INTRA expectation: FLAG on `inline-status` — a living-by-default set (INV-4) never annotates a rule with
its current implementation status.
