# Interfaces — implementer (V3 corpus: obligation-alignment PASS)

This implementer cites the owner's contracts and states only its own side.

- **`INTF-ZR-SOURCE`** — implements the owner's event source. Cites `owner · owner/docs/behavior · INTF-SOURCE`.

## External references

| Name          | Owner set-path        | Owner UUID                           |
| ------------- | --------------------- | ------------------------------------ |
| `INTF-SOURCE` | `owner/docs/behavior` | 11111111-1111-4111-8111-111111111111 |

V3 expectation: `ok` (aligned) — the cited owner UUID resolves and the name matches the owner's.
