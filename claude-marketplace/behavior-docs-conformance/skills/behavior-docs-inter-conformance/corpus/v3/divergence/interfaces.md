# Interfaces — implementer (V3 corpus: genuine-divergence FAIL)

The implementer claims to implement an owner element whose UUID resolves to NO owner definition —
it OMITS or CONTRADICTS the owner's real contract (a fabricated / removed obligation).

## External references

| Name         | Owner set-path        | Owner UUID                           |
| ------------ | --------------------- | ------------------------------------ |
| `INTF-GHOST` | `owner/docs/behavior` | 99999999-9999-4999-8999-999999999999 |

V3 expectation: `FAIL` (reconciliation failure) — the cited owner UUID resolves to no owner
definition; distinct from the stale-name warning case.
