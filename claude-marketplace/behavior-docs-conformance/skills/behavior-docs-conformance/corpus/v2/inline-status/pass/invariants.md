# Invariants — sample (inline-status PASS fixture, V2 #15)

- **`INV-1`** — The core delivers every accepted event at least once (durable across restart).

V2 expectation: CLEAN — the rule is stated as-if-true (living by default, INV-4).
