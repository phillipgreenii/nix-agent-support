# Journeys — sample (extent-traceability FAIL fixture)

- **`JOURNEY-1`** — An operator injects an event and it is delivered exactly once.

V2 expectation: FLAG on `extent-traceability` — no invariant in this set governs "exactly once", so
the extent-in claim is untraceable.
