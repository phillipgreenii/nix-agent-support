# Journeys — sample (extent-traceability PASS fixture)

- **`JOURNEY-1`** — An operator injects an event and it is delivered at least once (`INV-1`).
- **`INV-1`** — Delivery is at-least-once.

V2 expectation: CLEAN — the extent-in claim traces to an invariant in the set.
