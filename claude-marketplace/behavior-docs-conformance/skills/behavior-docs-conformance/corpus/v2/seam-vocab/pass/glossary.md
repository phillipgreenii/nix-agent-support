# Glossary — sample (seam-vocab PASS fixture, INV-20)

This set cites `some-repo · owner/docs/behavior · INV-EVT-1` for **event** and either inherits that
meaning verbatim or renames its own distinct concept:

- **event** — inherited from the owner (a typed message routed to a handler); used unchanged here.
- **audit row** — this set's own local audit-log entry (renamed, not conflated with **event**).

V2 expectation: CLEAN — the borrowed term is inherited; the distinct concept is renamed (INV-20).
