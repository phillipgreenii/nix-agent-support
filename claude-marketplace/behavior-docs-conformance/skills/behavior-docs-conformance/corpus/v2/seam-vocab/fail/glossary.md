# Glossary — sample (seam-vocab FAIL fixture, INV-20)

This set cites `some-repo · owner/docs/behavior · INV-EVT-1` for **event**, then silently REDEFINES
it locally:

- **event** — a row in this set's local audit log.

V2 expectation: FLAG on `seam-vocab` — a borrowed (cited) term is redefined without inherit-or-rename
(INV-20 reference-seam vocabulary).
