# Scope — sample (substitution PASS fixture, extent well-formedness)

- **Extent** — This set covers how the core routes typed events from sources to handlers. It defers
  the scheduler and the concrete handlers to downstream sets (the floor).

V2 expectation: CLEAN — the extent is a behavioral statement that survives the substitution test.
