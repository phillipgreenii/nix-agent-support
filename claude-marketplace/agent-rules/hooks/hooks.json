{
  "description": "Injects always-on personal agent rules into every session as SessionStart additionalContext",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [{ "type": "command", "command": "agent-rules-session-start" }]
      }
    ]
  }
}
