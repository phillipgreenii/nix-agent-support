[auto-nudge: pa-monitor, idle %[1]dm] This Claude Code session has been idle for %[1]d minutes and its prompt cache may be close to expiring. If you were pulled away mid-task and there is genuine pending work, invoke the session-wrapup:wrap-up-session skill now with args: "auto-trigger" so it gets committed, tracked, and handed off cleanly before the cache expires. If there is nothing pending, no action is needed.
