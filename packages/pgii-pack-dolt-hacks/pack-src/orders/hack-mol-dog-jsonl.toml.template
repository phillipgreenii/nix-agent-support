# HACK 11: workaround for mol-dog-jsonl schema-rename bug. Replaces the
# upstream order (suppressed via city.toml [[orders.overrides]]). See
# HACKS.md HACK 11 + assets/imports/pgii-dolt-hacks/scripts/hack-mol-dog-jsonl.sh.
[order]
description = "HACK 11: mol-dog-jsonl replacement — patches upstream type→issue_type schema bug at runtime."
trigger     = "cooldown"
interval    = "15m"
exec        = "${SCRIPTS_DIR}/hack-mol-dog-jsonl.sh"
timeout     = "180s"
enabled     = true
