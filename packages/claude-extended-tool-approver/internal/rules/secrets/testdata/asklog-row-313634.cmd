bd close pg2-ia640.2 --reason "Fixed CETA secrets false-positive where firstSecretRef ran IsSecret on the grep/rg PATTERN + jq value/filter args. RELOCATED skipGrepPattern/skipJqValueFlags + tables from safecmds into internal/cmdparse (new argflags.go, exported SkipGrepPattern/SkipJqValueFlags); updated the 3 safecmds call sites (no dup). firstSecretRef now filters args via secretCandidateArgs before IsSecret. FIXED the unstripped -e/-f explicit-pattern branch (grep -e/-f .env no longer leaks .env). Added a COMMAND-AWARE rg-only value-flag table (-g/--glob/--iglob/-t/-T/--type-add/-r/-M/--sort*/-E/--engine/--pre/--ignore-file/-d) — reviewer-recommended reconciliation: kept it SEPARATE from grep's table because rg's -r/-E/-T are booleans in grep, so folding would create a false-NEGATIVE (grep -r pattern /secrets/x.json). Exempted the bare jq filter (guarded against -f/--from-file to avoid the same FN). TDD: FP->Abstain (grep/rg .env, grep -e/-f .env, jq --arg x .env, jq '.credentials' data.json); regression STAY Ask (grep password .env, grep Host ~/.ssh/config, jq '.token' auth.json). Gates: go test -race PASS (secrets/safecmds/cmdparse + whole module), prek clean, nix flake check exit 0. Landed ff-merge to agent-support main @72010d36." --actor "b2cf2436-575f-4534-8590-9b04941bdeef" 2>&1 | head -2
