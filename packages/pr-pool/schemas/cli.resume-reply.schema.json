{
  "$id": "cli.resume-reply",
  "title": "INTF-CLI socket resume verb reply: the resolved gate and its state after the call — idempotent, so resuming an already-resumed gate is a no-op success.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "gate", "set"],
  "properties": {
    "schemaVersion": { "const": "1" },
    "gate": { "type": "string", "enum": ["quota_paused", "cicd_down"] },
    "set": { "type": "boolean" },
    "mtime": { "type": "string" }
  }
}
