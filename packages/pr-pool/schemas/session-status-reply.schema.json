{
  "$id": "session-status-reply",
  "title": "session-status acceptance ack (core -> handler)",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "id", "accepted"],
  "properties": {
    "schemaVersion": { "const": "1" },
    "id": { "type": "string" },
    "accepted": { "type": "boolean" }
  }
}
