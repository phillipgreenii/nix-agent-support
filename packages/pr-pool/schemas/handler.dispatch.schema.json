{
  "$id": "handler.dispatch",
  "title": "INTF-HANDLER dispatch (core -> handler)",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "id", "event"],
  "properties": {
    "schemaVersion": { "const": "1" },
    "id": { "type": "string" },
    "event": { "$ref": "event" }
  }
}
