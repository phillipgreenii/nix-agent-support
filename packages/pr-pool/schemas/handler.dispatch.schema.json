{
  "$id": "handler.dispatch",
  "title": "INTF-HANDLER dispatch (core -> handler)",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "id", "event", "callback"],
  "properties": {
    "schemaVersion": { "const": "1" },
    "id": { "type": "string" },
    "event": { "$ref": "event" },
    "callback": { "type": "string" }
  }
}
