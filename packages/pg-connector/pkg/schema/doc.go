// Package schema is a placeholder in the Tier-1 core packet. A sibling
// packet ("generic pr entity/capability") populates it with the pr entity
// type's shared JSON shape. pkg/schema, pkg/provider, and pkg/scriptout are
// the only packages importable across backend boundaries — see this
// module's layout-convention check in cmd/pg-connector.
package schema
