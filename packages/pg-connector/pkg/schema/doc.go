// Package schema holds the shared JSON wire shapes for each capability
// pg-connector supports — pr today (see pr.go), issue/ci/scm as they're
// eventually built. pkg/schema, pkg/provider, and pkg/scriptout are the
// only packages importable across backend boundaries — see this module's
// layout-convention check in cmd/pg-connector.
package schema
