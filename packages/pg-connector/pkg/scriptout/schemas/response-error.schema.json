{
  "$id": "response-error",
  "title": "scriptout wire response: error branch (stdout, exit 1 or 2-7)",
  "type": "object",
  "additionalProperties": false,
  "required": ["protocolVersion", "error"],
  "properties": {
    "protocolVersion": { "type": "integer" },
    "schemaVersion": { "type": "integer" },
    "error": { "$ref": "error" }
  }
}
