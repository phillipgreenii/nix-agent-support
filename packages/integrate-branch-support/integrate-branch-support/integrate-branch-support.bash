# shellcheck shell=bash
# Reserved for the detector's core logic (primary-branch resolution, signal
# gathering, strategy selection) as testable functions, split out of
# integrate-branch-support.sh. Intentionally empty in this scaffolding task;
# later tasks populate it. No top-level code — mkBashScript sources this
# file before the .sh body.
