{
  "$id": "handler.preShutdown-reply",
  "title": "INTF-HANDLER preShutdown reply (never deferred; there is no queued dispatch to defer)",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "id", "outcome"],
  "properties": {
    "schemaVersion": { "const": "1" },
    "id": { "type": "string" },
    "outcome": { "type": "string" }
  }
}
