{
  "$id": "handler.preShutdown",
  "title": "INTF-HANDLER preShutdown (core -> handler, once per process lifetime)",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "id"],
  "properties": {
    "schemaVersion": { "const": "1" },
    "id": { "type": "string" }
  }
}
