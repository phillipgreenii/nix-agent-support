{
  "$id": "cli.pause",
  "title": "INTF-CLI socket pause verb (INV-LIFE-2, Task 3.9): a caller that already holds a connection to a running core pauses one of the two named gates without ever causing a core to start (ADR 0036). Omitting `gate` defaults to operator_paused, the same default cmd/pg-router's file-direct `pause [<gate>]` subcommand uses.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion"],
  "properties": {
    "schemaVersion": { "const": "1" },
    "gate": { "type": "string", "enum": ["operator_paused", "cicd_down"] }
  }
}
