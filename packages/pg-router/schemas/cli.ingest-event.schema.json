{
  "$id": "cli.ingest-event",
  "title": "INTF-CLI ingest-event request (manager -> core callback)",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "id", "events"],
  "properties": {
    "schemaVersion": { "const": "1" },
    "id": { "type": "string" },
    "events": { "type": "array", "minItems": 1, "items": { "$ref": "event" } }
  }
}
