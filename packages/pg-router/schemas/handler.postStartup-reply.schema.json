{
  "$id": "handler.postStartup-reply",
  "title": "INTF-HANDLER postStartup reply (never deferred; there is no queued dispatch to defer)",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "id", "outcome"],
  "properties": {
    "schemaVersion": { "const": "1" },
    "id": { "type": "string" },
    "outcome": { "type": "string" }
  }
}
