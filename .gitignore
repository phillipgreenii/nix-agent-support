result
.direnv
.superpowers/
.worktrees/

# compiled Go binary checked in by accident during package migration; rebuilds
# dirty the working tree on every `go build` in the package dir
/packages/pa-monitor/pa-monitor
/packages/pa-monitor-decorator-gc/pa-monitor-decorator-gc

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Gas City
.beads/*
!.beads/config.yaml
!.beads/metadata.json

# Claude Code per-session worktrees (runtime state, created by EnterWorktree)
.claude/worktrees/

# sp-bd-bridge per-session cache (local-only)
.perles/

# pre-commit-config.yaml is a symlink into /nix/store regenerated by the
# devShell's pre-commit hook setup. Different store paths on different
# machines.
.pre-commit-config.yaml
